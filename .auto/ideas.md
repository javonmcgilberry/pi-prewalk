- When Pi exposes an extension-facing signal that says a threshold compaction will continue queued/tool work, replace Prewalk's `#hostCompaction`, `#checklistRun`, and native-compaction replay branches. The current `session_compact.willRetry` flag is insufficient because it describes aborted-turn retry, not queue/tool continuation; removing these branches now breaks planner checkpoint replay and unfinished executor checklist recovery.
- After Prewalk's compile-time Pi packages align with the updated runtime, evaluate `ExtensionContext.scopedModels` as the model source for configuration and route resolution instead of repeated global `modelRegistry.getAvailable()`/`find()` calls. Verify configured fallback models remain reachable within the resolved scope.
- Extract compaction event adaptation from `registerPrewalkEvents` only if it reduces the current high fan-out without recreating lifecycle state; keep `HostEventCorrelation` neutral and `ContextPressureController` as the semantic owner.
