# Web-search extension identification

The active Pi package list in `/Users/javonmcgilberry/.pi/agent/settings.json`
includes `npm:pi-web-access`. This package exposes the `web_search` tool used in
this session. The earlier identification as `pi-web-search` was incorrect.
